# Testing

We use the Amen test runner alongside the DashKite assertion library to evaluate the correctness of the Genie Pug preset. Our testing approach focuses on verifying that the configuration evaluates properly and that the compilation pipeline produces the correct output.

You can invoke the test suite using the standard Genie test command:

```bash
npx genie test
```
